export struct Translations {
    // main window sidebar
    app-name: string,
    library: string,
    all-books: string,
    reading: string,
    completed: string,
    favorites: string,
    // main window header
    books: string,
    contents: string,
    grid: string,
    list: string,
    // Dialog
    cancel: string,
    accept: string,
    delete: string,
    quit: string,
    save: string,
    edit: string,
    select: string,
    select-all: string,
    select-none: string,
    disable: string,
    enable: string,
    // Content dialog
    field-title: string,
    field-author: string,
    field-language: string,
    new-content: string,
    new-book: string,
    // People dialog
    new-person: string,
    field-name: string,
    field-role: string,
    new-role: string,
    // Book optional fields
    field-original-title: string,
    field-publication-date: string,
    field-isbn: string,
    field-notes: string,
    field-people: string
}

export global Tr {
    in property <Translations> t: {
        app-name: "App",
        library: "Library",
        all-books: "All books",
        reading: "Reading",
        completed: "Completed",
        favorites: "Favorites",
        books: "Books",
        contents: "Contents",
        grid: "Grid",
        list: "List",
        cancel: "Cancel",
        accept: "OK",
        delete: "Delete",
        quit: "Quit",
        save: "Save",
        edit: "Edit",
        select: "Select",
        select-all: "Select All",
        select-none: "Select None",
        disable: "Disable",
        enable: "Enable",
        field-title: "Title",
        field-author: "Author",
        field-language: "Language",
        new-content: "New Content",
        new-book: "New Book",
        new-person: "New Person",
        field-name: "Name",
        field-role: "Role",
        new-role: "New Role",
        field-original-title: "Original title",
        field-publication-date: "Publication date",
        field-isbn: "ISBN",
        field-notes: "Notes",
        field-people: "People"
    };
}
