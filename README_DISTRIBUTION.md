# Distribution Instructions

## Setup Instructions

1. Ensure you have the necessary dependencies installed.
2. Run `make build-release` to build the release.
3. After building, run `make zip` to create a zip archive of the build.
